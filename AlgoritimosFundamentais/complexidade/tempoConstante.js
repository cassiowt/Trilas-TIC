// Exemplo de algoritmo O(1) - A complexidade é constante.
function retornaPrimeiroElemento(array) {
    return array[0];
}

const lista = [1, 2, 3, 4, 5];
const primeiroElemento = retornaPrimeiroElemento(lista);

console.log("Primeiro elemento da lista: " + primeiroElemento);
